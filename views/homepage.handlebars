{{#each posts}}
    <div class="container">
      <div class="card">
        <div class="card-title"> {{title}} </div>
        <div class="card-text"> {{text}} </div>
      </div>
    </div>
  
{{/each}}